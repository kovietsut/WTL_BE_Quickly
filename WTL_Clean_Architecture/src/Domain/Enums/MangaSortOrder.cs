namespace Domain.Enums
{
    public enum MangaSortOrder
    {
        Default = 0,
        Latest = 1,    // Sort by published date descending (newest first)
        Oldest = 2     // Sort by published date ascending (oldest first)
    }
} 